# --- MySQL (Hostinger / producción) ---
# El host exacto aparece en hPanel → Bases de datos MySQL (a menudo localhost en el servidor)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=u313974416_vc_analytics
MYSQL_PASSWORD=tu_contraseña
MYSQL_DATABASE=u313974416_vc_analytics

# Código institucional (texto plano). Se hashea al iniciar el servidor si la BD está vacía.
ACCESS_CODE=SuCodigoSecreto2026

# API Express
PORT=3001
SESSION_HOURS=8
CORS_ORIGIN=*

# Proxy Vite en desarrollo (opcional)
VITE_API_URL=http://localhost:3001

# --- Gemini (opcional) ---
GEMINI_API_KEY=
